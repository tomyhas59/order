import React from "react";
import MenuListRenderer from "../components/MenuListRenderer";

const Japanese = ({ data }) => {
  return <MenuListRenderer data={data.japanese} />;
};

export default Japanese;
