import React from "react";
import MenuListRenderer from "../components/MenuListRenderer";

const Chinese = ({ data }) => {
  return <MenuListRenderer data={data.chinese} />;
};

export default Chinese;
